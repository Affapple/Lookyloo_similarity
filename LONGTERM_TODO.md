# Long-Term TODO

Items deferred for the real/production version:

- Add a confidence threshold slider in the UI.
- Add "why this matched" details (hash distance/score explanation).
- Add basic rate limiting per IP on upload endpoints.
- Add an audit log (timestamp, filename, result count).
- Add a one-click seed/reset script for reproducible demo state.
- Add a health page showing backend and DB status.
