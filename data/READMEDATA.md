this folder contains three sets of images. They each zip is one image with various alterations to test perceptual hashes. 

We will probably add all but one to our vector database and then see that the others are returned when that one is used as a query.